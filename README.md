# kanun-digital

Static landing page for https://kanun.digital.

## Local preview

Open `index.html` directly in a browser, or run a simple static server:

```bash
python3 -m http.server 8000
# then visit http://localhost:8000
```

## Deploy

Repo includes Netlify-style `_redirects` and `_headers` files.
